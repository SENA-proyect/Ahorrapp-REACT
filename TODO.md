# TODO - Vincular ruta exportar (App.jsx)

- [x] Crear componente `Frontend/src/components/exportar.jsx` que permita al usuario elegir formato/tipo/fechaInicio/fechaFin y llame a `exportarDatos`.
- [x] Ajustar `Frontend/src/components/exportar.jsx` para que exporte un componente React (default export) consumible desde `App.jsx`.
- [x] Actualizar `Frontend/src/App.jsx` para importar el componente y agregar la ruta `path="/exportar"`.
- [x] Ejecutar build/lint para verificar que compila (`npm run build` o `npm run dev` según corresponda).
